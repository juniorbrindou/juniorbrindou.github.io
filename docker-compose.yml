version: '3.8'

services:
  brindoujunior-site:
    build: .
    container_name: brindoujunior-portfolio
    restart: always
    ports:
      - "8081:80"
    networks:
      - site-network

networks:
  site-network:
    driver: bridge
